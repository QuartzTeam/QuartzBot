# Two Discord applications run in this one process.

# --- PrismBot: release announcements ---
# Use the ORIGINAL application's token here. It owns every release message
# already posted, and Discord routes button interactions to the application
# that owns the message — a new application would break the language toggle on
# past announcements. Rename that application to PrismBot in the developer
# portal; the token does not change.
ANNOUNCER_TOKEN=your-original-discord-bot-token
DISCORD_CHANNEL_ID=channel-for-release-announcements
# Must match the secret configured in GitHub -> repo Settings -> Webhooks.
# The same secret and the same /webhook URL work for every repo you add.
GITHUB_WEBHOOK_SECRET=your-webhook-secret
# Optional but recommended on shared hosts (Render): raises the GitHub API
# rate limit for the language button's release lookups from 60/hr (per shared
# IP) to 5000/hr. Fine-grained token, public repos, read-only.
# GITHUB_TOKEN=github_pat_...

# --- PrismAI chat (leave AI_TOKEN unset to run announcements-only) ---
# A SECOND, new Discord application, so PrismAI shows up as its own member.
# Requires MESSAGE CONTENT INTENT enabled on that application.
AI_TOKEN=your-new-ai-bot-token
OPENROUTER_API_KEY=your-openrouter-key
DATABASE_URL=postgres://postgres:postgres@localhost:5432/discordbot
# Comma-separated = primary + fallbacks when the primary is rate-limited,
# e.g. OPENROUTER_MODEL=nousresearch/hermes-3-llama-3.1-405b:free,meta-llama/llama-3.3-70b-instruct:free
# OPENROUTER_MODEL=nousresearch/hermes-3-llama-3.1-405b:free
# SHORT_TERM_LIMIT=10
# SUMMARY_EVERY=20
# KNOWLEDGE_FILE=knowledge.md
# SYSTEM_PROMPT=custom persona override (default persona + safety prompt is built in)
